Tasks:

Test avg pooling
Max pooling
Gramshcmidt
url testing
Find ESG dataset to test four models

Model mean:

we take a tweet, we extract keywords, then we take the average of all the embeddings and we apply the concept to it


Model_mean 0 :

we take the mean of keywords 
we measure the cosine similarity with each of the three vectors(avg_E, avg_S, avg_G) then we take the 
max of all three to determine which one is better

Model_mean 1: Max words, Mean dictionnary

Model_mean 2: 

Look up typehinting
docstrings python
#Info
#Parameters
#Returns


Spark instead of dataframe
Learn to use FLASK